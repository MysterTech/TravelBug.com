import React, { Component } from 'react';
import {listByUser} from "./apiTrip";
import { isAuthenticated } from "../auth";
import { Redirect } from "react-router-dom";

class TripsByUserId extends Component {
    constructor(props) {
        super(props);
        this.state = {  };
    }
    render() {
        return (
            
        );
    }
}

export default TripsByUserId;